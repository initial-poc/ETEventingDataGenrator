spanner:
  instance:
    id: instance-1
  database:
    id: database-1
  project:
    id: sab-order-service-sbx-7006

GOOGLE_APPLICATION_CREDENTIALS: ./cred.json

max:
  version:
    replicate: 2
  data:
    limit: 70

scheduler:
  pool:
    size: 50
  delay:
    interval: 60   # In sec


tps: 50


